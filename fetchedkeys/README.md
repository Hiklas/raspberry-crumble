This directory holds keys fetched from the nodes (specifically the master node) for copying to other machines.

The .gitignore file in this directory prevents and of these copied keys getting committed.